1 select e.first_name,e.last_name ,e.superior_emp_id, b.name,d.name from employee e,department d,branch b where b.branch_id in (1,2) and e.dept_id=d.dept_id and e.assigned_branch_id=b.branch_id;
2  select * from acc_transaction where to_char(txn_date,'yyyy')=2002;
3)  select * from customer c,account a where c.cust_id in ( select cust_id from account group by cust_id,open_date,last_activity_date having count(*)>1);